import { ConvertToKmPipe } from './convert-to-km.pipe';

describe('ConvertToKmPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToKmPipe();
    expect(pipe).toBeTruthy();
  });
});
